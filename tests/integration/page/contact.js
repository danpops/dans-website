const { NAV_LOGO, NAV_MENU, PAGE_MARQUEE } = require('.')

module.exports = {
  pageName: '#contact',
  CONTACT_WINDOW: '#contact-window',
  CONTACT_LINK: '#contact-link',
  NAV_LOGO,
  NAV_MENU,
  PAGE_MARQUEE
}
